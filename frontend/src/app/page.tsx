export default function HomePage() {
  return (
    <section className="py-16 text-center">
      <h1 className="text-4xl font-semibold tracking-tight">
        Orchestrate AI agents as a team
      </h1>
      <p className="mx-auto mt-4 max-w-xl text-slate-400">
        Run agents on local CLI runtimes or hosted APIs, compose them into workflows on a
        visual canvas, and ship real deliverables inside project workspaces.
      </p>
    </section>
  );
}
